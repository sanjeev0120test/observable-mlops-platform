feat: Phase 0 - repository foundation (23-UC AIOps+MLOps platform)

Establishes the complete project skeleton for all 23 use cases:

- Directory structure: 40+ namespaced folders created
- pyproject.toml: Ruff + Black lint config, pytest settings
- .pre-commit-config.yaml: hadolint, ruff, pre-commit-hooks
- .gitignore: Python, DVC, secrets, Terraform, Jupyter
- README.md: full 23-UC table, architecture, stack, secrets guide
- requirements.txt: pinned versions for all 30+ libraries

Data layer:
- 5 synthetic data generators with deterministic seeds + anomaly injection
- data/dvc.yaml: DVC pipeline stages for all 5 generators + validate_all
- scripts/validate_data.py: CI validation of generated files

Eval framework:
- eval/metrics.py: MetricSpec definitions for all 23 UCs + THRESHOLDS
- eval/scorer.py: compute_score(), run_eval_gate(), aggregate_all_results()

Infrastructure:
- infra/docker-compose/: Stack A (mlops-core), Stack B (observability),
  Stack C (services) + Loki/Tempo/FluentBit configs + Grafana datasources
- infra/kind/: 3 cluster configs (policy, serving, pipelines)
- infra/helm/: kyverno, keda, kserve values

Services (stub FastAPI apps, syntax-validated):
- anomaly-detector, drift-monitor, alert-correlator, predictive-scaler,
  self-healing, runbook-agent, cost-optimizer
  Each has Dockerfile + FastAPI health endpoint + Prometheus metrics

AIOps:
- Falco custom rules (UC7), OPA model_promotion + self_healing Rego
- Kyverno policies: require-labels, disallow-privileged
- n8n self-healing workflow JSON (UC6 stub)

Observability:
- OTEL collector config: traces to Tempo, metrics to Prometheus, logs to Loki
- Prometheus + Alertmanager + SLO fast-burn + drift + cost anomaly rules

Scripts:
- setup-dagshub.sh: MLflow + DVC remote config with no-token fallback
- setup-kind.sh: Kind cluster bootstrap (serving/pipelines/policy profiles)
- setup-ollama.sh: TinyLlama pull with health wait
- publish-portal.sh: Eval dashboard HTML generator

CI:
- .github/workflows/00-pr-validate.yml: lint, structure check, eval
  framework unit tests, data generator smoke tests, workflow inventory

Backstage:
- catalog-info.yaml: 26 entities (23 UC services + 3 ML models)

Eval framework validated: all 23 UCs registered, scorer pass/fail verified.

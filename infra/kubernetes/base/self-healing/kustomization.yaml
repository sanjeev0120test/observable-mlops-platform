apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Production-hardened self-healing service (UC6): deployment + PDB + NetworkPolicy.
# Apply with: kubectl apply -k infra/kubernetes/base/self-healing/
resources:
  - deployment.yaml
  - pdb.yaml
  - networkpolicy.yaml

commonLabels:
  app.kubernetes.io/part-of: observable-mlops-platform
